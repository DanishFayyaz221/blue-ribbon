# Yarn 4 defaults to Plug'n'Play, which removes node_modules. Next.js native
# deps (sharp) and this project's AGENTS.md workflow both expect a real
# node_modules tree, so pin the classic linker.
nodeLinker: node-modules
